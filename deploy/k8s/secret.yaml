apiVersion: v1
kind: Secret
metadata:
  name: kitematic-secrets
  labels:
    app: kitematic
type: Opaque
stringData:
  # Override via kubectl apply -f <(envsubst < secret.yaml) or external secrets operator
  KITEMATIC_REDIS_URL: "redis://redis-service:6379/0"
